# apiVersion: v1
# kind: Service
# metadata:
#   name: angular-service
#   labels:
#     app: angular
# spec:
#   selector:
#     app: angular
#   type: NodePort
#   ports:
#   - nodePort: 31021
#     port: 80
#     targetPort: 80

apiVersion: v1
kind: Service
metadata:
  name: angular-service
  labels:
    app: angular
spec:
  selector:
    app: angular
  type: NodePort
  ports:
  - nodePort: 31019
    port: 80
    targetPort: 80