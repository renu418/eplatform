# apiVersion: v1
# kind: Service
# metadata:
#   name: python-service
#   labels:
#     app: python
# spec:
#   ports:
#   - protocol: TCP 
#     port: 5031
#     targetPort: 5031
#   type: ClusterIP
#   selector:
#     app: python
#     name : python-service
    

apiVersion: v1
kind: Service
metadata:
  name: python-node
  labels:
    app: pythonapp
spec:
  selector:
    app: pythonapp
  type: NodePort
  ports:
  - nodePort: 31022
    port: 5031
    targetPort: 5031